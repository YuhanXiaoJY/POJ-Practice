#include<iostream>
#include<cstring>
#include<stack>
using namespace std;
void SuffixCompute(char a[])
{
    int len = strlen(a);
    stack<char> stk;
    for(int i = 0; i< len; i++)
    {
        
    }
}
int main()
{
    cin.sync_with_stdio(false);
    cout.sync_with_stdio(false);
    int N;
    cin >> N;
    while(N--)
    {
        char a1[100], a2[100];
        cin.getline(a1, 100);
        cin.getline(a2, 100);
        SuffixCompute(a1);
        
        Suffix()
    }
    system("pause");
    return 0;
}